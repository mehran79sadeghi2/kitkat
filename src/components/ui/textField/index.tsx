import { FC } from "react";
import { tv } from "tailwind-variants";

const styles = tv({
    slots: {
        root: 'w-full h-10 ',
    }
})

const TextField: FC = () => {

    return (
        <div></div>
    )
}

export default TextField;
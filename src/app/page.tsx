import Button from "@/components/ui/button";
import SearchRoundedIcon from "@/components/ui/icons/search";

export default function Home() {
  return (
    <div>
      <Button  variant="outline" fullWidth scheme="primary">تائید و ادامه<SearchRoundedIcon /></Button>
    </div>
  );
}
